# Step01 改善提案（優先度つき）

最終更新: 2025-12-24

## 1) アクションの厳密ピンニング（中）
- `actions/checkout@v4` 等をコミット SHA にピン留めし、サプライチェーンリスクを低減。

## 2) 環境値の集中管理（中）
- `NODE_VERSION` を 01-ci の `env` にのみ定義し、再利用先へ `inputs` 経由で渡す設計へ（必要に応じて）。

## 3) セットアップ重複の方針ドキュメント化（低）
- 「ジョブの独立性により重複は意図的」という方針を README／ワークフロー先頭コメントへ追記し、将来の誤解を防止。

## 4) 将来の拡張余地（低）
- テストワークフロー追加（`07-ci-test.yml` など）
- Lint／Format を matrix（OS／Node）で検証するスモーク拡張
